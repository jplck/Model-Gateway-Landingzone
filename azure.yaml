# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: aigw-landing-zone
metadata:
  template: aigw-landing-zone@0.0.1-beta
infra:
  provider: bicep
  path: infra
  module: main
hooks:
  preprovision:
    shell: sh
    run: scripts/preprovision.sh
    interactive: true
  postprovision:
    shell: sh
    run: scripts/postprovision.sh
